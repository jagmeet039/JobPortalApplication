spring:
  application:
    name: UserAuthService
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:userAuthDB
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080

